from django.apps import AppConfig


class ClassifyTaskConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'classify_task'
    verbose_name = "A2. Clasificación de tareas"
